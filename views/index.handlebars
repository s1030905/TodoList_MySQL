<a href="/todo/new" class="btn btn-primary mb-3">Create</a>
<ul class="list-group">
  {{#each todos}}
  <li class="list-group-item">
    <div class="row">
      <div class="col-auto me-auto">
        <input class="align-middle" type="checkbox" name="isDone" {{#if this.isDone }} checked {{/if}} disabled>
        <h4 class="d-inline ms-2 align-middle">
          {{ this.name }}
        </h4>
      </div>
      <div class="col-auto">
        <a class="btn btn-secondary" href="./todo/{{ this.id }}">detail</a>
        <a class="btn btn-success" href="./todo/{{ this.id }}/edit">edit</a>
        <form action="./todo/{{ this.id }}?_method=DELETE" method="POST" style="display: inline;">
          <button class="btn btn-danger" type="submit">delete</button>
        </form>
      </div>
    </div>
  </li>
  {{/each}}
</ul>